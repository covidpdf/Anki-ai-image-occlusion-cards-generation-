import type { FC } from 'react'

const Export: FC = () => (
  <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <h2 className="text-lg font-semibold text-slate-900">Export Decks</h2>
    <p className="mt-2 text-sm text-slate-600">
      Package your finished cards into Anki-compatible deck formats for seamless import.
    </p>
  </section>
)

export default Export
